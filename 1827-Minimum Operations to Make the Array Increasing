class Solution {
    public int minOperations(int[] nums) {
        int operations = 0;
        int prev = 0;
        for (int num : nums) {
            if (num <= prev) {
                operations += prev - num + 1;
                num = prev + 1;
            }
            prev = num;
        }
        return operations;
    }
}
